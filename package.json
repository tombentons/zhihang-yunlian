{
  "name": "zhihang-yunlian",
  "version": "1.0.0",
  "description": "智航云连 - 港航集疏运数字服务平台",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "build:frontend": "cd frontend && npx vite build",
    "seed": "cd backend && node src/seed.js",
    "start": "cd backend && node src/seed.js && node src/index.js",
    "dev:backend": "cd backend && node src/index.js",
    "dev:frontend": "cd frontend && npx vite --host",
    "build": "npm run build:frontend",
    "postinstall": "cd backend && npm install && cd ../frontend && npm install && npm run build:frontend"
  },
  "engines": {
    "node": "18.x"
  }
}
